{
  "name": "site_tech_trend_bz",
  "version": "1.0.0",
  "private": true,
  "description": "Single-page client website for Tech Trend Mobile BZ.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 5177 --directory src",
    "start": "python3 -m http.server 4177 --directory dist",
    "lint": "node scripts/validate.mjs",
    "test": "node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
