# AGENTS.md

This is a small static client-site project. Keep it dependency-light and sales-focused.

- Use `site_` naming for future client-site repositories.
- Do not invent client facts; use WOP/RDS data, spreadsheets, or public client profiles.
- Keep WhatsApp as the primary conversion path.
- Run `npm run build` and `npm run lint` before deployment.
- Deploy the generated `dist/` directory to AWS Amplify.
- Do not commit secrets or local deployment zips.
